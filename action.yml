name: "Build Android Release New Tag."
branding:
  icon: "package"
  color: "red"
description: "Build Android release APK and upload as a github release match the created new tag"
author: "HeCodes2Much"
inputs:
  GITHUB_TOKEN:
    description: "Github secret token to allow automated publishing of APK to release."
runs:
  using: "docker"
  image: "Dockerfile"
